/*
Search for multiple room_type_ids
$(".chk_room_type").on('click', function() {

  room_type_ids = [];

  $("input:checkbox[name='booking[room_type_ids][]']:checked").each(function(){
      room_type_ids.push($(this).val());    
  });

  $.ajax({
        url: '/rooms/search',
        data: {room_type_ids: room_type_ids},
        //dataType: 'json',
        success: function( response ) {

          $("#display_rooms").html( response );
        }
  });
});*/

$(".chk_room_type").on('click', function() {

  room_type_id = $(this).val();

  if( true === $(this).is(':checked') ) {

    $.ajax({
          url: '/rooms/search_by_room_type_id',
          data: {room_type_id: room_type_id},
          //dataType: 'json',
          success: function( response ) {
            $("#room_"+ room_type_id ).html( response );
          }
    });
  } else {
    $("#room_"+ room_type_id ).html('');
  }

});

$('.datepicker').datepicker();
$(document).on("focus", "[data-behaviour~='datepicker']", function(e){
    $(this).datepicker({"format": "yyyy-mm-dd", "weekStart": 1, "autoclose": true});
});